swagger-api:
  version: "1.0"

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ylab_db
    username: postgres
    password: 251254
#  jpa:
#    show-sql: true
#    hibernate:
#      ddl-auto: update
  sql:
    init:
      schema-locations: classpath*:schema.sql
      mode: always

server:
  port: 8091
  servlet:
    context-path: /app
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: health,info